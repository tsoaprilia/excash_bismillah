<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Izin Internet (jika perlu komunikasi online) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Izin Bluetooth (jika pakai printer thermal) -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>

    <!-- Izin Penyimpanan untuk Android 11+ -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>

    <!-- Izin Penyimpanan untuk Android 10 ke bawah -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"/>

    <!-- Izin untuk Media di Android 10+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>

    <application
        android:requestLegacyExternalStorage="true"
        android:label="excash"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- konfigurasi aplikasi lainnya -->
    </application>

</manifest>
